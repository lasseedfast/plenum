Execute a read-only PostgreSQL query against the Swedish Parliament database.

Use this for structured questions about metadata: party breakdowns, aggregations, speaker
statistics, comparisons over time, and content-based counts via full-text search.

✅ WHEN TO USE
- Counting or ranking: "how many speeches per party?", "the 10 most active speakers in 2020?"
- Aggregations and joins: activity per party over time, speeches joined with member demographics
- Full-text aggregations: "how many speeches per party mentioned AI?"
- Outcomes of proposals (yrkanden): what a party proposed and how the chamber decided

❌ WHEN NOT TO USE
- Semantic or conceptual search → `vector_search`
- Exact word or phrase search → `search_speeches`
- Fetching whole documents → `fetch_speeches` or `fetch_document`

<!-- Generated by scripts/generate_schema_prompt.py; edit the column comments, not this. -->

**Schema.** The only columns that exist; a query naming another fails.
Lines marked · are what the column name alone would not tell you.

    speeches            id·PK, text, section_title, sequence INT, activity_type
                        speaker_name, party, person_id, date DATE, year INT
                        session_year INT, title, debate_id, is_reply BOOL, summary
                        tags TEXT[], search_vector tsvector, arguments TEXT[]
                        · section_title: what the debate was about — the topic to group or search by
                        · activity_type: debate form; 66% filled, and '-' and '' occur
                        · session_year: 2016 means the 2016/17 session
                        · title: the protocol's title, not the speech's — use section_title
                        · arguments: model-extracted claims, not the member's words
    people              person_id·PK, birth_year, gender, party, constituency, name
                        active BOOL
                        · birth_year: text, not a number — cast before comparing
                        · gender: man, kvinna, Okänt
                        · constituency: 65% filled
    debates             id·PK, date DATE, summary, num_talks INT, talk_ids TEXT[]
    documents           doc_id·PK, session_label, subtype, committee, date DATE
                        session_year INT, title, text, parties TEXT[]
                        author_names TEXT[], num_proposals INT, search_vector tsvector
                        · subtype: Enskild, Kommitté, Parti, Flerparti motion; '-' occurs
                        · committee: 27 codes — SoU, TU, UbU, MJU...
                        · session_year: 2011 means the 2011/12 session
                        · parties: case varies — fold with upper()
    document_authors    doc_id·PK, ordinal·PK INT, person_id, name, party
                        · ordinal: signing order; 0 is the first author
                        · party: case varies in older rows — fold with upper()
    document_proposals  id·PK, doc_id, ordinal INT, number, text
                        committee_recommendation, chamber_decision, handled_in
                        · id: {doc_id}:{ordinal}
                        · number: text, not a number
                        · committee_recommendation: 76% filled
                        · chamber_decision: 77% filled; values below
                        · handled_in: committee report, e.g. 2001/02:TU2

Joins: `speeches.person_id = people.person_id` · `speeches.debate_id = debates.id` ·
`document_authors.doc_id = documents.doc_id` · `document_proposals.doc_id = documents.doc_id`.

Also worth knowing:

- `documents` has no plain `year` — use `session_year`.
- Cast dates when selecting them: `date::text`.
- Select `person_id` alongside speeches so results link back to a speaker.
- `summary`, `tags` and `arguments` are written by a model from the source text.
  Use them to find things; quote only `text`, which is what was actually said.

**Full-text search, never LIKE.** Use
`WHERE search_vector @@ websearch_to_tsquery('swedish', '...')` — it uses the GIN index
and handles quoted phrases, `OR`, `-` to exclude, and stemming. It covers the text on
`speeches`, and title + proposals + text on `documents`.

- **Never** `text @@ ...` or `LIKE`/`ILIKE` on `text`: no index, whole-table scan, and wrong
  matches ('ai' hits 'Thai').
- **Never** put `search_vector @@ ...` inside `SELECT`, `SUM` or `CASE` — it then runs per
  row without the index and takes 30–60 seconds. For two counts, filter in two CTEs:

      WITH a AS (SELECT id, party FROM speeches
                 WHERE search_vector @@ websearch_to_tsquery('swedish', 'q1')),
           b AS (SELECT id FROM speeches
                 WHERE search_vector @@ websearch_to_tsquery('swedish', 'q2'))
      SELECT a.party, COUNT(*) total, COUNT(b.id) matches
        FROM a LEFT JOIN b USING (id) GROUP BY 1

**Party values.** The real codes are S, M, SD, C, V, KD, MP, L. `speeches.party` also holds NULL and
`TALMANNEN`, `FÖRSTE VICE TALMANNEN`, `ANDRE VICE TALMANNEN`, `TREDJE VICE TALMANNEN` — the presiding chair, not a party. Exclude those from per-party counts.

**`chamber_decision` values.** Filtering on the plain "approved" value alone is the commonest
way to get a badly wrong answer; here it covers barely one percent of rows.

- `Bifall` — approved outright.
- `Avslag` — rejected.
- `Delvis bifall` — partly approved.
- `= utskottet`, `=utskottet` — no outcome of its own — the chamber followed the committee, so `committee_recommendation` has to be read to learn what happened. **Both spellings occur.**
- `NULL` — not decided, or not applicable.

To count what a party actually got through, take the approved and partly-approved values
*and* the rows deferring to the committee, resolved via `committee_recommendation`. If you
give one number, say which of these it covers.

**Examples**

    -- speeches on a topic per party, using the index (query words in svenska)
    SELECT party, COUNT(*) c FROM speeches
      WHERE party IN ('S', 'M', 'SD', 'C', 'V', 'KD', 'MP', 'L')
        AND search_vector @@ websearch_to_tsquery('swedish', '<topic OR synonym>')
      GROUP BY party ORDER BY c DESC

    -- how one party's proposals fared
    SELECT p.chamber_decision, p.committee_recommendation, COUNT(*) c
      FROM document_proposals p JOIN documents d USING (doc_id)
      WHERE d.parties && ARRAY['S']
      GROUP BY 1, 2 ORDER BY c DESC

