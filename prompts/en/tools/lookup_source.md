Recall the stored grounding text for sources you have already seen.

Once registered, search results in your message history are compacted to a single line:
`[src:ID] Speaker (Party) date — heading — preview`. The full text is kept server-side.

Call this ONLY when you actually need the underlying text — to quote verbatim, or to verify
a specific claim. For most claims the compacted line plus your own notes are enough.

**Maximum 5 source ids per call.** Pick the few you really need; bodies are truncated to
keep your context lean.
